{
  "name": "attendance-fcm-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone FCM relay for the attendance Flutter app.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/seed-demo-users.js",
    "seed:demo-users": "node src/seed-demo-users.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.3",
    "firebase-admin": "^12.7.0"
  }
}